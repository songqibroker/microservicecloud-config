


spring:
  profiles:
    active: 
    - dev
    
---

server:
  port: 8201
spring:
  profiles: dev
  application:
    name: microservicecloud-config-client

service:
  client: config-dev
    
      
      
---
server:
  port: 8082
spring:
  profiles: test
  application:
    name: microservicecloud-config-client
    
    
service:
  client: config-test
    